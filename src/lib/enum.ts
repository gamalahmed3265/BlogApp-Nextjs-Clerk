export enum Routes {
  ROOT = "/",
  DASHBOARD = "dashboard",
  SIGNIN = "sign-in",
  POSTS = "posts",
  NEWDASHBOARD = "new",
  EDIT = "edit",
}
export enum Environments {
  PROD = "production",
  DEV = "development",
}
