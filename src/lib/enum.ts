export enum Routes {
  ROOT = "/",
  DASHBOARD = "dashboard",
  SIGNIN = "sign-in",
  POSTS = "posts",
  NEWDASHBOARD = "new",
}
export enum Environments {
  PROD = "production",
  DEV = "development",
}
